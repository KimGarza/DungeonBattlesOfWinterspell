#include "GenerateNpcs.h"

void GenerateNpcs::Generate() {

	if (locName_ == "Moonlight Caverns") {
		//std::shared_ptr<NpcTrader>
	}
	// map of string, vector npcs
	// check locName against the map to find the matching key, then generate
	// npcs from file that correlate with the names
}
